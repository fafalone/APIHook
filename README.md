# APIHook

A generic API hooking module for twinBASIC. Includes trampoline to call original proc.

Written by the Claude LLM, then adapted to use WDL declares and call the original function in the demo via delegate.

Requires the "Windows Development Library for twinBASIC" package.
